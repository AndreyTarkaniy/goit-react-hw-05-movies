// import { MovieCard } from 'components/movieCard/movieCard';

// export const MoviesList = ({ movies }) => {
//   return (
//     <ul>
//       {movies &&
//         movies.map(movie => <MovieCard key={movie.idMovie} {...movie} />)}
//     </ul>
//   );
// };
